const pool = require("../db.js");

async function getRecipes() {
  // return all recipes (id, name, category and difficulity)
}

async function getRecipe() {
  // get everything about a recipe, including tools, ingredients, nutrition, steps
}

async function createRecipe() {
  // add to recipe table, as well as
}

module.exports = {};
